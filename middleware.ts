// Temporarily disabled for development - no authentication middleware
export default function middleware() {
  // No authentication checks during development
}

export const config = {
  matcher: [],
}